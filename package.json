{
  "name": "pdf-to-text",
  "version": "1.0.0",
  "description": "PDF to Text converter using Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler pages build --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  }
}
