{
  "name": "pdf-to-text",
  "version": "1.0.0",
  "description": "PDF to Text converter using Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "mkdir -p dist && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^3.11.7",
    "unpdf": "^0.0.15"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  }
}
